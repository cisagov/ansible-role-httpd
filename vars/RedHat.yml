---
# vars file for RedHat

# The packages to install for httpd
httpd_package_names:
  - httpd
  - mod_auth_gssapi
  - mod_authnz_pam

# The name of the httpd systemd service
httpd_service_name: httpd
